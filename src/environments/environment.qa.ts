// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    urlBackend:       '',
    urlExperienceAPI:'https://operationaladministrationqa.finsuite.com.mx/',
    envRegion: 'us-east-1',
    envUserPoolId: '', // qa Pool Id - ID grupo
    envUserPoolWebClientId: '',  // qa app client id  pool de aplicaciones   - id cliente de la aplicacion
    envLogin:'',
    endPassword: ''

  };
  